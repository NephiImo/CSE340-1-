<%# Unit 5, Login Process activity
    Modified further in Unit 5 %>

<% if (title) { %>
<h1><%= title %></h1>
<% } else { res.redirect('/') } %>

<%- messages() %>

<% if (locals.errors) { %>
<ul class="message warning">
  <% locals.errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <%  }) %>
</ul>
<% } %>

<h2>Welcome <%= locals.accountData.account_firstname %> <%= locals.accountData.account_lastname %></h2>
<h3>Account Type: <%= locals.accountData.account_type  %></h3>

<p>You're logged in.</p>
<a href="/account/update/<%= locals.accountData.account_id %>" title="Click to update your account information">Edit Account Information</a>

<% if(locals.accountData.account_type != 'Client'){ %>
   <h3>Inventory Management</h3>
<p><a href="/inv/">Manage Inventory</a></p> 
<% } %>